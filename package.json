{
  "name": "devlens",
  "version": "0.1.0",
  "private": true,
  "description": "Clean-room developer browser workspace with Focus UI and network-to-REST replay.",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test",
    "check": "node --check src/server.mjs && node --check src/url-policy.mjs && node --check src/public/app.js && node --check src/public/app-core.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
